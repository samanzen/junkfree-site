# Supabase (shared with junkfree-agent) — powers /api/lead capture and
# lets the SEO agent's published content appear on the site.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Real business contact info (falls back to placeholders if unset)
NEXT_PUBLIC_PHONE=+16045551234
NEXT_PUBLIC_PHONE_DISPLAY=(604) 555-1234
NEXT_PUBLIC_EMAIL=hello@junkfree.ca
